(measure-time
 (read
  (concat
   "("
   (shell-command-to-string "mongo contacts --quiet --eval 'db.contacts.find().forEach(function (doc) {print(\"(\\\"\" + doc.NAME + \"\\\" . \\\"\" + doc.EMAIL.escape() +\"\\\")\");})'")
   ")")
  )
 )

