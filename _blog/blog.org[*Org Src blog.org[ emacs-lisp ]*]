(define-abbrev-table 'scimax-ordinal-abbrev-table
  '(("rd" "(insert ▮)" nil :system t)

    )

  "abbrev table for numbers like 3^{rd}"
  :regexp "\\([0-9]+\\)"
  :case-fixed t
  :enable-function (lambda () )
  )
