(print (macroexpand '(defun f [(a "an int")
                               (b "an int" :default 4)
                               (c "more ints" :rest)]
                       "some doc"
                       (* a b))))
